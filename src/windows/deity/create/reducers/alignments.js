const initialState = {
  EvilGood: ['Evil', 'Neutral', 'Good'],
  ChaosLaw: ['Chaotic', 'Neutral', 'Lawful']
};

module.exports = function (state, action) {
  switch(action) {
    default:
      return state;
  }
};
